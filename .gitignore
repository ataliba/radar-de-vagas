# Consumido pelo docker-compose.yml (interpolação de ${VAR}) — segredos
# reais (token JobSync, usuário/senha do basic auth). Nunca commitar.
/.env*
!/.env.example

# Tem IP interno da LAN de deploy (registry local). Local-only.
/build.sh
